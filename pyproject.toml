[project]
name = "hkust-superpod-auto"
version = "0.1.0"
description = "One-command toolkit for HKUST SuperPod: VPN, SSH tunnel, tmux sessions, Claude Code"
requires-python = ">=3.13"
dependencies = [
    "playwright>=1.58.0",
    "pyotp>=2.9.0",
    "vpn-slice>=0.16.1",
]
